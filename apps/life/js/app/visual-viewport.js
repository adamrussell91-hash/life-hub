/** Re-export the shared hub keyboard / visual-viewport contract. */
export {
  VV_KEYBOARD_OPEN_PX,
  VV_BASELINE_SHRINK_PX,
  VV_HEIGHT_STICK_PX,
  attachVisualViewportInset,
  notifyChatViewport,
  detachVisualViewportInset
} from '../../../../packages/design-kit/js/visual-viewport.js';
