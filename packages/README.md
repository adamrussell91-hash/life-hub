# packages/

Umbrella shared trees. Life Hub still boots from the repo root (Pages `dist/` + `netlify/functions`).

| Path | Role |
|------|------|
| `design-kit/` | Frozen kit copy. Deploy still uses repo-root `design-kit/` until remount. |
