# Checkpoint 07 — 2026-09-01

## Verdict
PASS

## Diff vs plan
- Done: matches `plan.md`'s "Slice 07 — kit-import from-anchor" exactly. Verified against **GitHub PR [#61](https://github.com/adamrussell91-hash/life-hub/pull/61)** ("Rewrite published kit imports only on from specifiers"), base `main`, head on `cursor/kit-import-from-anchor`, **OPEN, mergeStateStatus=CLEAN, mergeable=MERGEABLE**, 4 files changed (+21/-11 in `plan.md`, +15/-0 new lib, +2/-10 in `prepare-web.mjs`, +24/-0 new test) — confirmed via `gh pr view 61 --json files` and independently via `git diff main...HEAD --name-status` (identical 4-file list, no PR-description trust required).
  - New `scripts/lib/rewrite-published-kit-imports.mjs` extracts the rewrite into `rewritePublishedKitSpecifiers()`, anchored to `KIT_FROM = /from (['"])((?:\.\.\/)+packages\/design-kit\/)([^'"]+)\1/g` — this is precisely checkpoint-06's Risk #1 fix and Next-step #3 ("tighten the regex to only match inside actual `import ... from '...'` / `export ... from '...'` statements"). The old regex matched any `../`-prefixed `packages/design-kit/...` string anywhere in a `.js` file, including inside comments or string literals; the new one requires a literal `from` keyword immediately before the quoted specifier.
  - `scripts/prepare-web.mjs`: the inline regex/rewrite logic (10 lines, including the `dirname`/`relative`/`fileURLToPath` machinery) is deleted and replaced with a single import + call to `rewritePublishedKitSpecifiers(source, next, publishRoot)`. Same path-arithmetic mechanism from checkpoint-06 (relative-path recompute via `node:path`), now scoped to `from` specifiers only.
  - New `tests/unit/rewrite-published-kit-imports.test.js`: two tests — (1) rewrites both `import ... from '...'` and `export ... from '...'` kit specifiers to the correct published-relative path; (2) explicitly asserts a plain string literal assignment and a `//` comment containing the same path text are left **byte-identical**, unrewritten. This is the exact false-positive scenario checkpoint-06 flagged as possible but untested.
  - `docs/consolidation/plan.md`: status bumped to v3.1, checkpoint-06 recorded PASS/merged (#60), new "Slice 07" section added, cwd banner line added (`Overseer cwd: ~/Projects/life-hub/.worktrees/umbrella-seed-slice-01 ... Do not use the primary life-hub checkout`), Next-action banner replaced with this checkpoint's paste-prompt (now consumed). Documentation-only.
  - Independently re-verified rather than trusting the PR body, run in this worktree (branch `cursor/kit-import-from-anchor`, PR #61's head): `npm run build` → exit 0; `npm test` → **1607 pass / 0 fail** (2 more than checkpoint-06's 1605, matching the 2 new unit tests added); ran the new test file directly (`node --test tests/unit/rewrite-published-kit-imports.test.js`) → both tests pass. Spot-checked actual `dist/` output after the build: `dist/js/app/render-medical.js` and `dist/js/core/time.js` both still import/export from `../../packages/design-kit/js/...` with correct relative depth — same published paths as checkpoint-06, produced by the new anchored mechanism.
- Drifted: none against Slice 07's stated scope.
- Blocked (correctly, per plan): no `life-hub2` retarget, no Teaching/Knowledge/Tasks fold, no secret rotation. Full diff is exactly the 4 files above — confirmed via `git diff --name-status` against `main`, not the PR description.

## Boundary check
- life-hub-data untouched: **yes** — `git diff main...HEAD | grep -i life-hub-data` returns only two prose lines: the pre-existing "non-goal locked" banner and this checkpoint's own quoted instructions inside `plan.md`. No file under `life-hub-data` appears anywhere in the diff.
- Single Adam session/auth path: **yes** — no file under `netlify/functions/**` appears in the 4-file changed list; the diff touches only `scripts/`, `tests/unit/`, and `docs/consolidation/plan.md`.
- Public student (or other public) routes still unauthenticated by design: **n/a** — Teaching isn't part of this repo/slice; no public-route code in the diff.
- Secrets / tokens blast radius: **ok** — `git diff main...HEAD -- netlify.toml` and `git diff main...HEAD --stat -- netlify/functions` are both **zero-line diffs**, diffed directly. Both of PR #61's unchecked test-plan boxes ("Confirm `git diff origin/main -- netlify.toml` is empty", "Confirm no new env vars or secrets") are **independently confirmed true** here.
- Design kit still single source: **yes** — `packages/design-kit/` source itself isn't touched; only the specifier-matching regex used by the publish-time rewrite changed (narrowed from "anywhere in the file" to "inside a `from` clause"), which tightens correctness without adding a second kit source.

## Deploy / env
- No env var names, secret strategy, `SITE_ORIGIN` values, `netlify.toml` `directory`/`included_files`, or `.github/workflows/pages.yml` changed by this slice — diffed directly against `main`.
- Function paths/filenames unchanged; `netlify/functions/` has zero diff lines.
- `dist/` publish shape verified unchanged by an actual build: same top-level layout as checkpoints 05/06 (`assets/, css/, index.html, js/, manifest.webmanifest, packages/, service-worker.js, vendor/`), and the checkpoint-05/06 resolve-check test (`tests/integration/static-server.test.js`) still passed inside the full 1607-test run, confirming the narrowed rewrite regex still produces resolvable imports for every real case in the tree.
- Process note (same drift flagged at checkpoints 04, 05, 06 — now worse again): the **primary `life-hub` checkout** on this machine is still on `cursor/life-protocol-pills-c87b`, which is now **91 commits behind `origin/main`** (up from 87 at checkpoint-06) and 4 commits ahead on unrelated work; `docs/consolidation/` there is still entirely untracked (`?? docs/consolidation/`), alongside Adam's separate uncommitted edits (`index.html`, `render-central-node.js`, `render-chat.js` and their tests). This checkpoint was again read from and written into the `.worktrees/umbrella-seed-slice-01` worktree — the primary checkout was only inspected read-only (`git branch`, `git status`, `git rev-list --count`), never touched. `plan.md` v3.1 now names this worktree explicitly as the overseer cwd in its status banner, which should stop future checkpoints from having to rediscover this each time, but the underlying primary-checkout gap itself is unresolved.

## Calendar / cross-domain
- No change in this slice — out of scope for Slice 07.

## Risks
1. The narrowed `KIT_FROM` regex (`/from (['"])((?:\.\.\/)+packages\/design-kit\/)([^'"]+)\1/g`) matches the literal token `from ` followed by a quoted `../packages/design-kit/...` path. It is not anchored to statement-start, so a specifier inside a dynamic `import('../../packages/design-kit/x.js')` call (no `from` keyword) would **not** be rewritten and would silently break at runtime post-publish, since dynamic imports use a different syntax than the static `import ... from` / `export ... from` forms this regex targets. No dynamic `import()` of kit paths exists in the current tree (confirmed via the full `dist/js` walk test passing), so this is latent, not active — but it's the same "regex approximates a language grammar" pattern as the risk this slice just fixed, one level down. Worth a one-line comment on `KIT_FROM` noting the dynamic-import gap, not a blocker.
2. Both PR #61 test-plan checkboxes independently confirmed true here are still visually unchecked on GitHub — same recurring pattern noted at checkpoints 03 through 06 for PRs #57–#60.
3. Primary-checkout drift (see Deploy/env) has now persisted across five checkpoints (04–07) and gotten worse each time (87 → 91 commits behind). `plan.md` v3.1 added the cwd banner as a partial mitigation for the *overseer's* confusion, but does nothing for the checkout itself — Adam's uncommitted work there is still sitting on a stale, unrelated branch.

## Next 3 steps (Cursor only)
1. Tick PR #61's two open test-plan boxes (both independently confirmed true above), then merge PR #61 into `main`; it's already CLEAN/MERGEABLE. After merge, update `plan.md` Status to flip "Kit-import from-anchor" from "in progress" to a merged/dated shipped state, mirroring the #55→#60 pattern.
2. Add a short comment above `KIT_FROM` in `scripts/lib/rewrite-published-kit-imports.mjs` documenting that it intentionally does not match dynamic `import(...)` calls, and grep the tree once for `import(` + `design-kit` to confirm zero live cases before the next slice adds more files under `apps/life/js/`.
3. Raise the primary-checkout drift with Adam directly (not just re-noted in a report): ask him to commit or stash `cursor/life-protocol-pills-c87b`'s in-progress work (`index.html`, `render-central-node.js`, `render-chat.js` + tests) and switch that checkout to `main`, or confirm he wants checkpoints to permanently target this worktree going forward — `plan.md` v3.1's new cwd banner already assumes the latter, so a matching decision line under `plan.md`'s Decisions section would resolve the ambiguity.
