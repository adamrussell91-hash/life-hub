# Checkpoint 08 — 2026-09-01

## Verdict
PASS

## Diff vs plan
- Done: matches `plan.md`'s "Slice 08 — umbrella shell" exactly. Verified against **GitHub PR [#62](https://github.com/adamrussell91-hash/life-hub/pull/62)** ("Umbrella shell: hub mounts, Life calendar source, Teaching CORS"), base `main`, head on `cursor/umbrella-shell`, **OPEN, mergeStateStatus=CLEAN, mergeable=MERGEABLE**, 22 files changed (+283/-48) — confirmed via `gh pr view 62 --json files` and independently via `git diff main...HEAD --name-status` (identical file list).
  - **Hub mounts (shell-only):** `apps/life/js/shell/hub-sections.js` (new) defines `HUB_SECTIONS` — Teaching has `origin: 'https://teaching-hub.adam-russell.com'`, Knowledge and Tasks have `origin: null`. `render-hub-sections.js` (new) renders a link-out button when an origin exists, otherwise nothing. `apps/teaching/README.md`, `apps/knowledge/README.md`, `apps/tasks/README.md` (all new) are the **only** files under those three directories — no code, no Functions, no Blobs. Confirmed via `find apps/teaching apps/knowledge apps/tasks -type f`: three README files, nothing else. `netlify/functions/` diff is 3 files, all under `_shared/`, zero new handler files — grepped the full diff for `blobs|arteaching|TEACHING_HUB_PASSPHRASE`: only hits are prose in READMEs/plan.md stating Blobs/secrets are explicitly **not** touched this slice.
  - **Life calendar source:** `apps/life/js/shell/calendar-sources.js`'s `CALENDAR_SOURCES` array changes from empty to one entry (`{ id: 'life', label: 'Life Hub', kind: 'logged-days', status: 'live' }`) — a static descriptor, not a new data path. Grepped the full diff for `loadLiveEvents`: the only hits are comment/doc-prose lines saying it's unchanged; the actual implementation file `apps/life/js/app/load-live-events.js` has **zero diff lines** against `main`.
  - **CORS allow-list:** new `netlify/functions/_shared/umbrella-origins.mjs` exports `UMBRELLA_APP_ORIGINS = ['https://life-hub.adam-russell.com', 'https://teaching-hub.adam-russell.com']` and `isAllowedRequestOrigin(origin, env)`, which checks the request's `Origin` header against `[SITE_ORIGIN, ...UMBRELLA_APP_ORIGINS]` (deduped). `_shared/http.mjs`'s `corsHeaders()` and `requireAllowedOrigin()` are rewired to call it instead of comparing against a single fixed `SITE_ORIGIN` string. One behavior change worth naming explicitly: `corsHeaders()` now echoes the **actual matched request origin** back in `access-control-allow-origin` (previously it echoed the fixed `SITE_ORIGIN` value) — this is the standard, correct pattern for a multi-origin allow-list with `access-control-allow-credentials: true` (a static header can't name two origins), and it's still gated by `isAllowedRequestOrigin` first, so it does not become an open reflector. Verified directly: `tests/unit/umbrella-origins.test.js` asserts `isAllowedRequestOrigin('https://foreign.example', {})` is `false` and Teaching's origin is `true`; `tests/integration/request-origin.test.js` adds a real handler-level test hitting `/api/session` with `origin: https://teaching-hub.adam-russell.com` and asserts the response is not `403`.
  - **Public student route table:** new `netlify/functions/_shared/public-student-routes.mjs` exports `PUBLIC_STUDENT_API_ROUTES` (six method+pattern pairs under `/api/published/*`, `/api/media/*/file`, `/api/html-app-ai`, and the legacy `/.netlify/functions/published-lesson*`) and `isPublicStudentApi(method, pathname)`. `tests/unit/public-student-routes.test.js` confirms the six student paths return `true` and four operator paths (`/api/auth`, `/api/repo/manifest`, `/api/curriculum`, `/api/lessons/week-1`) return `false`.
  - `scripts/lib/rewrite-published-kit-imports.mjs`: one-line addition — a comment on `KIT_FROM` noting it only matches static `from '...'` specifiers, not dynamic `import()`. This is checkpoint-07's next-step #2, picked up unprompted; not part of Slice 08's stated scope but harmless documentation.
  - `docs/consolidation/plan.md`: status bumped to v4.0 language already reflected in what I read (checkpoint-07 recorded PASS/merged #61, Slice 08 section added). Documentation-only.
  - Independently re-verified rather than trusting the PR body, run in this worktree (branch `cursor/umbrella-shell`, PR #62's head): `npm run build` → exit 0; `npm test` → **1617 pass / 0 fail**, matching the PR's claimed 1617/1617.
- Drifted: none against Slice 08's stated scope.
- Blocked (correctly, per plan): no `life-hub2` retarget, no Teaching Functions/Blobs copied, no `arteaching-hub` retirement, no secret rotation. Full diff is exactly the 22 files listed above.

## Boundary check
- life-hub-data untouched: **yes** — `git diff main...HEAD | grep -in life-hub-data` returns only pre-existing/prose lines (the locked non-goal banner and this checkpoint's own quoted instructions inside `plan.md`). No file under `life-hub-data` appears in the diff.
- Single Adam session/auth path: **yes** — `netlify/functions/` diff touches only `_shared/http.mjs` (rewired to the new origin-list helper) and two new `_shared/` files; no new function handlers, no new passphrase/session env names. `UMBRELLA_SESSION_COOKIE` / `UMBRELLA_SESSION_SECRET_ENV` imports in `http.mjs` are unchanged.
- Public student (or other public) routes still unauthenticated by design: **yes** — the new `public-student-routes.mjs` table exists and is unit-tested, matching the plan's "public student API route table so `/api/published/*` stays unauthenticated when Teaching handlers land." One note: `isPublicStudentApi` is not yet **called** from anywhere else in the codebase (grepped `netlify/` and `apps/` for its name — only its own definition and test file reference it) — this is consistent with the plan's own framing ("stays unauthenticated **when** Teaching handlers land"; no such handlers exist in this repo yet), so there is nothing left ungated today, but it also means this table currently enforces nothing. Tracked as a risk below, not a blocker.
- Secrets / tokens blast radius: **ok** — `git diff main...HEAD -- netlify.toml` is a **zero-line diff**, diffed directly. Grepped the full diff for `env\.[A-Z_]+|process\.env\.[A-Z_]+`: the only env reference touched is the pre-existing `env.SITE_ORIGIN`, now read once into `umbrella-origins.mjs` instead of twice inline in `http.mjs` — no new env var names introduced anywhere. Both of PR #62's unchecked test-plan boxes ("Confirm `git diff origin/main -- netlify.toml` is empty", "Confirm no new env vars or secrets") are **independently confirmed true** here.
- Design kit still single source: **yes** — `packages/design-kit/` untouched; only the pre-existing rewrite-regex file gained a comment.

## Deploy / env
- No env var names, secret strategy, `SITE_ORIGIN` values, `netlify.toml` `directory`/`included_files`, or `.github/workflows/pages.yml` changed by this slice — diffed directly against `main`.
- CORS behavior change (see Diff vs plan): `access-control-allow-origin` now reflects the matched request origin instead of a fixed value, gated by the new allow-list. This is a real, intentional behavior change (needed for multi-origin support) rather than drift — flagged explicitly rather than buried, per this checkpoint's mandate to catch exactly this class of change.
- Function paths/filenames unchanged outside `_shared/`; no new handler files.
- Process note (persists, still worsening): the **primary `life-hub` checkout** is still on `cursor/life-protocol-pills-c87b`, now **94 commits behind `origin/main`** (up from 91 at checkpoint-07), with the same uncommitted work (`index.html`, `render-central-node.js`, `render-chat.js` + tests) still sitting there untouched. This checkpoint was again read from and written into the `.worktrees/umbrella-seed-slice-01` worktree only; the primary checkout was inspected read-only (`git branch`, `git status`, `git rev-list --count`) and not modified.

## Calendar / cross-domain
- Progress toward consolidated calendar: **partial** — `CALENDAR_SOURCES` now names Life Hub logged days as the first live source (descriptor only; `loadLiveEvents` itself is unchanged, so no new data path was introduced). Teaching/Knowledge/Tasks remain absent from the registry, matching the plan's explicit scope for this slice ("No other-hub APIs").
- Hub-rail mounts for Teaching/Knowledge/Tasks are navigational only (link-out or static placeholder text) — no cross-domain data flow yet.

## Risks
1. `isPublicStudentApi()` / `PUBLIC_STUDENT_API_ROUTES` is defined and unit-tested but not wired into any actual request-handling code path yet (no caller outside its own test file). It correctly enforces nothing today because no Teaching handlers exist in this repo to need gating — but it also means the table's correctness has only ever been checked against itself, not against a real handler's auth-guard logic. When Teaching's Functions land, the very first thing to verify is that whatever auth middleware they use actually calls `isPublicStudentApi()` before applying the session gate, not just that the table's patterns look right in isolation.
2. The CORS-reflection behavior change in `corsHeaders()` (origin echoed instead of a fixed value) is correct for the stated multi-origin need, but it's the kind of change that's easy to over-broaden later — e.g. a future contributor adding a third origin to `UMBRELLA_APP_ORIGINS` without realizing `access-control-allow-credentials: true` is still set, or (worse) swapping the allow-list check for a wildcard/regex "convenience" match. Worth a one-line comment on `isAllowedRequestOrigin` or `UMBRELLA_APP_ORIGINS` warning that this list is exact-match only, credentials-bearing, and should never become a substring/prefix match — cheap now, before it's someone else's landmine.
3. Both PR #62 test-plan checkboxes independently confirmed true here (`netlify.toml` empty diff, no new env vars) are still visually unchecked on GitHub, plus two more boxes that need Adam/Cursor's manual click-through (rail sections open, calendar shows Life Hub) since those aren't covered by the automated suite — same recurring documentation-lag pattern noted at every checkpoint from 03 through 07.
4. Primary-checkout drift (see Deploy/env) has now persisted across six checkpoints (04–08) and keeps growing (87 → 91 → 94 commits behind). This is the fourth consecutive checkpoint flagging it.

## Next 3 steps (Cursor only)
1. Manually click through PR #62's two remaining unchecked-by-tooling boxes ("Rail: Teaching / Knowledge / Tasks sections open", "Calendar Shared sources shows Life Hub") in a real browser against the built `dist/`, tick all four open boxes (the two above plus the netlify.toml/env-var ones independently confirmed here), then merge #62; it's already CLEAN/MERGEABLE. After merge, update `plan.md` Status to flip "Umbrella shell" from "in progress" to a merged/dated shipped state, mirroring the #55→#61 pattern.
2. Add a one-line comment on `UMBRELLA_APP_ORIGINS` / `isAllowedRequestOrigin` in `netlify/functions/_shared/umbrella-origins.mjs` documenting that the list must stay exact-match (no wildcard/prefix matching), since `access-control-allow-credentials: true` makes a broadened match a real cross-origin credential leak, not just a CORS nuisance — before the next hub's origin gets added to the list.
3. Raise the primary-checkout drift with Adam directly, same ask as checkpoint-07: commit or stash `cursor/life-protocol-pills-c87b`'s in-progress work and either switch that checkout to `main`, or add a decision line to `plan.md`'s Decisions section formally naming this worktree as the permanent overseer cwd (the status banner already assumes this; the Decisions section doesn't yet record it as agreed).
