# Checkpoint 06 — 2026-09-01

## Verdict
PASS

## Diff vs plan
- Done: matches `plan.md`'s "Slice 06 — published kit-import check" exactly. Verified against **GitHub PR [#60](https://github.com/adamrussell91-hash/life-hub/pull/60)** ("Assert published design-kit imports resolve after remount"), base `main`, head `e23205e` on `cursor/dist-kit-import-resolve` (2 commits ahead of `origin/main`), **OPEN, mergeStateStatus=CLEAN, mergeable=MERGEABLE**, 3 files changed (+55/-14).
  - `scripts/prepare-web.mjs`: `rewritePublishedKitImports()` no longer does a fixed string swap (`'../../../../packages/design-kit/'` → `'../../packages/design-kit/'`, the exact mechanism checkpoint-05's Risk #1 flagged as silently fragile). It now matches any `(?:\.\.\/)+packages/design-kit/...` specifier and recomputes the relative path from each published file's actual directory to the real `dist/packages/design-kit/...` target via `node:path`'s `relative()` — path arithmetic instead of a literal-depth assumption.
  - `tests/integration/static-server.test.js`: new test `'every published design-kit import in dist/js resolves'` walks the built `dist/js/**/*.js` tree, extracts every `packages/design-kit` import specifier, and asserts (`fs.access`) that each one resolves to a real file — this is precisely the follow-up test checkpoint-05 recommended and closes that risk.
  - `docs/consolidation/plan.md`: status bumped to v3.0, checkpoint-05 recorded PASS/merged (#59), new "Slice 06" section added, Next-action banner replaced with this checkpoint's paste-prompt (now consumed). Documentation-only.
  - Independently re-verified rather than trusting the PR body, run in the already-checked-out worktree `.worktrees/umbrella-seed-slice-01` (branch `cursor/dist-kit-import-resolve`, PR #60's head — not the main checkout, see Deploy/env note): `npm run build` → exit 0; `npm test` → **1605 pass / 0 fail**; ran the new test directly (`node --test tests/integration/static-server.test.js`) → `✔ every published design-kit import in dist/js resolves`. Inspected actual `dist/` output: top level unchanged (`assets/, css/, index.html, js/, manifest.webmanifest, packages/, service-worker.js, vendor/`); spot-checked `dist/js/core/time.js` and `dist/js/app/render-medical.js` import `packages/design-kit/js/hub-filter-menu.js`, which exists on disk.
- Drifted: none against Slice 06's stated scope.
- Blocked (correctly, per plan): no `life-hub2` retarget, no Teaching/Knowledge/Tasks fold, no secret rotation. Full diff is exactly 3 files (`docs/consolidation/plan.md`, `scripts/prepare-web.mjs`, `tests/integration/static-server.test.js`) — confirmed via `git diff --name-status origin/main origin/cursor/dist-kit-import-resolve`, not the PR description.

## Boundary check
- life-hub-data untouched: **yes** — no file in the diff touches `life-hub-data`; the only `life-hub-data` string in the diff is prose inside `plan.md`'s existing non-goal-locked banner line, carried over unchanged.
- Single Adam session/auth path: **yes** — no file under `netlify/functions/**` appears in the changed-file list at all (confirmed by the 3-file diff above); grepped the full diff for `PASSPHRASE|SESSION_SECRET|GITHUB_TOKEN` — zero hits outside the plan-prose line quoting the checkpoint's own instructions.
- Public student (or other public) routes still unauthenticated by design: **n/a** — Teaching isn't part of this repo/slice; no public-route code in the diff.
- Secrets / tokens blast radius: **ok** — `git diff origin/main origin/cursor/dist-kit-import-resolve -- netlify.toml` is a **zero-line diff**, diffed directly. `git diff --stat -- netlify/functions` is also empty. Both of PR #60's unchecked test-plan boxes ("Confirm `git diff origin/main -- netlify.toml` is empty", "Confirm no new env vars or secrets") are **independently confirmed true** here.
- Design kit still single source: **yes** — `packages/design-kit/` source itself isn't touched; only the *import-rewrite mechanism* that publishes it into `dist/` changed, and it changed to be more correct (path arithmetic vs. a fixed string), not to introduce a second source.

## Deploy / env
- No env var names, secret strategy, `SITE_ORIGIN` values, `netlify.toml` `directory`/`included_files`, or `.github/workflows/pages.yml` changed by this slice — diffed directly against `origin/main`.
- Function paths/filenames unchanged; `netlify/functions/` has zero diff lines.
- `dist/` publish shape verified unchanged by an actual build: same top-level layout as checkpoint-05 (`assets/, css/, index.html, js/, manifest.webmanifest, packages/, service-worker.js, vendor/`), and the new resolve-check test now guards every `packages/design-kit` import in `dist/js/` on every future build, not just this one.
- Process note (same drift flagged at checkpoints 04 and 05, still open, one level worse now): the **main checkout's** `docs/consolidation/` on this machine is now **entirely untracked** (`git status` shows `?? docs/consolidation/`) on the currently checked-out branch (`cursor/life-protocol-pills-c87b`), which is **87 commits behind `origin/main`** and 4 commits ahead on unrelated work. This checkpoint was read from and written into the `.worktrees/umbrella-seed-slice-01` worktree (PR #60's head), which has the correct current copies — the main checkout was left untouched throughout, including Adam's separate uncommitted work there (`index.html`, `render-central-node.js`, `render-chat.js` and their tests, per his own Cursor session). Not a merge blocker for #60, but the gap between "where OVERSEER.md says cwd should be" and "where the actual current plan.md lives" keeps widening — worth a deliberate fix, not just a repeated note.

## Calendar / cross-domain
- No change in this slice — out of scope for Slice 06.

## Risks
1. `rewritePublishedKitImports()`'s new regex (`/(?:\.\.\/)+packages\/design-kit\/([^'"]+)/g`) matches any `../`-prefixed `packages/design-kit/...` specifier regardless of depth, which is the intended fix — but it also means a stray or malformed specifier elsewhere in a `.js` file (e.g. inside a comment or string literal that isn't actually an import) would be silently rewritten too, since the match isn't anchored to an `import`/`from` statement the way the new *test's* regex is (`from ['"](...)['"]`). Low risk today (no such string exists), but the rewrite regex and the test's verification regex aren't the same pattern, so a future false-positive in the rewrite wouldn't necessarily be caught by the test in the same way it would be introduced. Not a blocker.
2. Both PR #60 test-plan checkboxes independently confirmed true here are still visually unchecked on GitHub — same recurring pattern noted at checkpoints 03, 04, and 05 for PRs #57, #58, #59.
3. Main-checkout `docs/consolidation/` drift (see Deploy/env) has now persisted across four checkpoints (04, 05, 06) and gotten worse — it flipped from "stale but tracked" to "untracked entirely" on a branch 87 commits behind main. Escalating this from a note to an actual next step below.

## Next 3 steps (Cursor only)
1. Tick PR #60's two open test-plan boxes (both independently confirmed true above), then merge PR #60 into `main`; it's already CLEAN/MERGEABLE. After merge, update `plan.md` Status to flip "Published kit-import check" from "in progress" to a merged/dated shipped state, mirroring the #55→#59 pattern.
2. Fix the main-checkout `docs/consolidation/` drift directly rather than re-noting it: either (a) have Adam switch that checkout to `main`/pull once his uncommitted `cursor/life-protocol-pills-c87b` work is committed or stashed by him, or (b) point future checkpoint runs at a dedicated worktree on `main`/the PR branch by default (as this and the prior checkpoint already had to improvise) so OVERSEER.md's "cwd = life-hub repo root" instruction stops silently meaning "whichever worktree happens to be current."
3. Tighten the new `rewritePublishedKitImports()` regex to only match inside actual `import ... from '...'` / `export ... from '...'` statements (same shape as the new test's verification regex) so the rewrite and the check can't drift apart — cheap now, before more files accumulate under `apps/life/js/`.
