# Review

| Severity | Finding | Reproduction | Fix commit | Verification |
| --- | --- | --- | --- | --- |
| Low | `docs/IMPLEMENTATION_STATUS.md` understated the current suite as 51 tests after the suite reached 52. | `actual=$(rg -o '[0-9]+ tests, [0-9]+ passed, [0-9]+ failed' docs/IMPLEMENTATION_STATUS.md); expected='52 tests, 52 passed, 0 failed'; test "$actual" = "$expected"` exited 1 with `actual: 51 tests, 51 passed, 0 failed`. | `f0cd18b` | The same assertion exited 0 with `actual: 52 tests, 52 passed, 0 failed`. |
| Important | The YAML parser pin was affected by prototype-pollution and quadratic merge-work advisories. | Real hostile `__proto__`, chained-merge, and repeated-alias inputs failed against the affected releases; repeated aliases took about 1.07 seconds on 4.1.1. | `a35ca83`, `dabb27d` | Exact 4.3.0; four hostile-parser tests pass; fresh audit reports 0 vulnerabilities. |
| Important | Weekly downsampling allocated every intervening day before enforcing 120 points. | A 1000-to-9000 span took about 2.53 seconds before rejection and failed the constant-space regression. | `97a1100` | Inclusive week count is checked first; the millennia regression rejects in under 1 ms in the full run. |
| Important | Calendar helpers accepted shape-valid impossible dates because UTC construction normalised them. | The new canonical helper import failed before implementation; impossible dates were then exercised across every date-key operation, search extension, and record validation. | `97a1100` | `time.js` owns semantic validation; time, search, records, validation, and trends focused suite passed 42/42. |
| Minor | Sydney autumn DST coverage was missing. | Added both real `02:30` representations (`+11:00` and `+10:00`), adjacent-day arithmetic, and timestamp-validation coverage. | `9bdc0f8` | Both characterization regressions pass without relying on an impossible local timestamp. |
| Minor | Unknown target day types produced `NaN` calories. | Typed-error regression initially reported “Missing expected exception.” | `9bdc0f8` | `getDayTargets` now throws `TypeError` before calorie arithmetic. |
| Minor | Effective-date coverage used only one target set. | Added three unsorted sets, exact boundaries, intervening dates, prehistory rejection, and an input snapshot. | `9bdc0f8` | Greatest applicable set is selected and caller input remains unchanged. |
| Minor | Legacy search results discarded their path and had no stable identity. | Canonical key/path and exact legacy-wrapper assertions initially received `undefined`/missing properties. | `9bdc0f8` | Results preserve `id`, `date`, `type`, and `snippet`, and add `path` plus `key: id ?? path`. |
| Confirmed configuration | Six supplied registry colours needed to replace provisional/previous values while Hammond remained unchanged. | Registry test initially showed all six colour mismatches and five provisional sources. | `dc45863` | Exact supplied colours/sources pass; the seven-agent roster excludes Sterling and preserves Hammond verbatim. |

Final verification: 63 tests passed, fixture validation reported 4 valid and 0 invalid files, exact `js-yaml` 4.3.0 resolved, `npm audit` reported 0 vulnerabilities, secret scans found no matches, and `git diff --check main...HEAD` passed. Central Node content was not modified in this fix wave.
