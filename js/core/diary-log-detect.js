/** Penelope-facing re-exports of the shared log-finalize detectors. */
export {
  PENELOPE_FORCE_DIARY_NUDGE,
  isDiaryFinalize,
  isLogFinalize,
  claimedDiaryFiling,
  shouldForcePenelopeDiaryProposal
} from './log-finalize-detect.js';
