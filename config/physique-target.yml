# Authoritative physique and strength targets live in config/targets.yml under `forecast`.
# load-physique-target.mjs reads that block. This file stays so Netlify included_files
# still resolves; do not put target numbers here.
